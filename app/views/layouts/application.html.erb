<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Snagagent" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  
  <body class="container mt-4">

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">SnagAgent</a>

    <div class="collapse navbar-collapse">
      <ul class="navbar-nav ms-auto">
              <% if user_signed_in? %>
                  <li class="nav-item">
                    <span class="nav-link">Signed in as <strong><%= current_user.email %></strong></span>
                  </li>

                  <% if current_user.admin? %>
                    <li class="nav-item">
                      <%= link_to "Users", admin_users_path, class: "nav-link" %>
                    </li>
                    <li class="nav-item">
                      <%= link_to "Add User", new_admin_user_path, class: "nav-link" %>
                    </li>
                  <% end %>

                  <li class="nav-item">
                    <%= link_to "Snags", snags_path, class: "nav-link" %>
                  </li>

                  <li class="nav-item">
                    <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: "nav-link" %>
                  </li>
                <% else %>
                  <li class="nav-item">
                    <%= link_to 'Sign in', new_user_session_path, class: "nav-link" %>
                  </li>
                  <li class="nav-item">
                    <%= link_to 'Sign up', new_user_registration_path, class: "nav-link" %>
                  </li>
      <% end %>

       
      </ul>
    </div>
  </div>
</nav>

<% flash.each do |key, message| %>
  <% bootstrap_class = case key
     when "notice" then "success"
     when "alert" then "danger"
     when "error" then "danger"
     else "info"
  end %>

  <div class="alert alert-<%= bootstrap_class %> alert-dismissible fade show mt-3 mx-3" role="alert">
    <%= message %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>

  

    <%= yield %>
  </body>
</html>
